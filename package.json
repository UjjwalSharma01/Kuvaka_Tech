{
  "name": "lead-scoring-backend",
  "version": "1.0.0",
  "description": "Backend service for scoring leads using rule-based logic and AI reasoning",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["lead-scoring", "ai", "gemini", "backend"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0"
  }
}
